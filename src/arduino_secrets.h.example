// Template: copy to src/arduino_secrets.h and fill in your own
// WiFi credentials. The real file is excluded from the repository
// via .gitignore.
#define SECRET_SSID "YOUR_WIFI_NAME"
#define SECRET_PASS "YOUR_WIFI_PASSWORD"
